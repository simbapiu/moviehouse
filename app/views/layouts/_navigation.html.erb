<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="#">MovieHouse</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <li class="nav-item active">
          <a class="nav-link" href="/store/">Home <span class="sr-only">(current)</span></a>
        </li>  
      <% else %>
        <li class="nav-item active">
          <a class="nav-link" href="/movies/">Home <span class="sr-only">(current)</span></a>
        </li>
      <% end %>
        <li class="nav-link"><%= link_to 'Show', carts_url %></li>
      <% if admin_signed_in? %>
      <li class="nav-item">
        <a class="nav-link" href="new">New movie</a>
      </li>
      <% end %>
      <% if user_signed_in? %>
        <li class="nav-link"><%= link_to 'Sing out', destroy_user_session_path, method: :delete %></li>
        
      <% elsif admin_signed_in? %>
        <li class="nav-link"><%= link_to 'Sing out', destroy_admin_session_path, method: :delete %></li>
      <% else %>
      <li class="nav-item"><%= link_to 'Sing in', new_user_session_path %></li>
      <li class="nav-item"><%= link_to 'Sing up', new_user_registration_path %></li>
      <% end %>
    </ul>
    <form class="form-inline my-2 my-lg-0">
      <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
      <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
    </form>
  </div>
</nav>